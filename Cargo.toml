[package]
name = "build-upload"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.2" }
reqwest = { version = "0.11", features = ["blocking", "json", "multipart"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
